services:
  backend:
    image: bpitest/skill-backend:stack
    restart: always
    ports:
      - 8080
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}"
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
