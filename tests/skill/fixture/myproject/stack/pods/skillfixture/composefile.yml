services:
  backend:
    image: bpitest/skill-backend:stack
    restart: always
    ports:
      - 8080
  db:
    image: postgres:16
    restart: always
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
